{% extends 'base.html.twig' %}
{% block stylesheets %}{% endblock %}
{% block main %}
    <h1>Gérer les villes</h1>
    <div class="rec">
<h3>Filtrer les villes</h3>
    <input class="form-control" type="text" placeholder="Rechercher une ville..." aria-label="Search">
        <button type="submit" class="btn btn-primary">Rechercher</button>
    </div>
    <table class="table">
        <thead class="thead-dark">
        <tr>
            <th scope="col">Ville</th>
            <th scope="col">Code Postal</th>
            <th scope="col">Actions</th>

        </tr>
        </thead>
        <tbody>
        {% for v in ville %}
            <tr>
                <th>{{ v.nom }}</th>
                <td>{{ v.cp }}</td>
            </tr>
        {% endfor %}
        <tr>

            {{ form_start(formVille) }}
            <td>{{ form_row(formVille.nom) }}</td>
            <td>{{ form_row(formVille.codePostal) }}</td>
        <td> <button type="submit" class="btn btn-primary">Ajouter</button></td>
            {{ form_end(formVille) }}

        </tr>
    </table>
{% endblock %}
